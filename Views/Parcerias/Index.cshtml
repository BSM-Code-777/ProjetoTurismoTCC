@model IEnumerable<turismoTCC.Models.Parceria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.beneficio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Localidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Premium)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.beneficio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Localidade.bairro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Premium.nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idParceria">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idParceria">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idParceria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
